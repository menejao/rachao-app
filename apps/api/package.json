{
  "name": "@rachao/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "import:players": "tsx src/scripts/import-jogadores-csv.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm --prefix ../.. -w @rachao/types run build && npm --prefix ../.. -w @rachao/utils run build && tsc -p tsconfig.test.json && node dist-test/apps/api/src/test-runner.js"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "@rachao/types": "0.1.0",
    "@rachao/utils": "0.1.0",
    "zod": "^3.24.4"
  },
  "devDependencies": {}
}
